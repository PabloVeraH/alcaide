version: 1
defaults:
  mode: enforcement
  block_threshold: high

rules:
  - id: test-jailbreak-rule
    category: jailbreak
    severity: high
    pattern_type: literal
    pattern: "ignore all previous instructions"
    enabled: true
